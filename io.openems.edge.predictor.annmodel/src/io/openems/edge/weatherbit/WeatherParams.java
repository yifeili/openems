package io.openems.edge.weatherbit;

/**
 * @author Jan Seidemann
 */
public enum WeatherParams {
	temp, clouds, ghi, solar_rad, uv, pod, precip, snow, vis, dhi, dni, elev_angle, azimuth, h_angle, dewpt, rh,
	app_temp, wind_dir, wind_spd, slp, pres, code;
}
